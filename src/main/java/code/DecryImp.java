package code;

public class DecryImp implements Decry{
    @Override
    public byte[] decry(byte[] bytes) {
        return new byte[0];
    }

    @Override
    public boolean isDecry() {
        return false;
    }
}

package code;

public class EncryImp implements Encry{
    @Override
    public byte[] encry(byte[] bytes) {
        return new byte[0];
    }

    @Override
    public boolean isEncry() {
        return false;
    }
}
